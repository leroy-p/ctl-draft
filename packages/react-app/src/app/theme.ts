export interface ITheme {
  palette: {
    primary: string
    secondary: string
    error: string
  }
}

export const theme: ITheme = {
  palette: {
    primary: '#aaaaaa',
    secondary: '#000000',
    error: '#ff0000',
  },
}
